# Gradle daemon options only. Project/version config lives in stonecutter.properties.toml.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# 26.1 requires the Gradle daemon to run on Java 25. Pin it explicitly (same as Anvil).
org.gradle.java.home=C:/Program Files/Zulu/zulu-25

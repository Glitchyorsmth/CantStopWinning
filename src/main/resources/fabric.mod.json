{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Fullscreen celebration overlay for Hypixel SkyBlock drops. Built on Anvil.",
  "authors": ["glitc"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": ["com.cantstopwinning.CantStopWinningClient"]
  },
  "depends": {
    "anvil": "*",
    "fabricloader": ">=0.18",
    "minecraft": "${minecraft}"
  }
}
